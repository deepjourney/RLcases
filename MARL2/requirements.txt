torch
numpy
matplotlib
tqdm
optuna
joblib
easydict
pygame
scikit-video
# stable-baselines3 不带 [extra]：[extra] 会拖进带 GUI/SDL 的 opencv-python，
# 在 macOS 上和 pygame 的 SDL 冲突刷一大堆无害警告。这里改用 headless 版的 opencv，
# 功能一样（项目只用 cv2 做图像处理，不开窗口），从源头消除冲突。
stable-baselines3
# ale-py 按平台区分：
#   - Mac Apple Silicon (arm64)：0.12 没有预编译 wheel，pip 会从源码现编，编出的版本
#     不带 Atari ROM（报 breakout.bin not found）。0.10.2 有现成 arm64 wheel 且自带 ROM。
#   - Linux/Colab (x86_64)：0.12 有现成 wheel + 自带 ROM，且 env 步进更快，用最新版。
ale-py>=0.10,<0.11 ; platform_machine == "arm64"
ale-py>=0.12       ; platform_machine != "arm64"
opencv-python-headless
